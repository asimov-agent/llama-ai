[pytest]
testpaths = tests
markers =
    install: host install tests that require `make install` artifacts and ~/models
    health: end-to-end health check (launch a tiny model, hit the endpoint with a prompt)
